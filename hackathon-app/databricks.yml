bundle:
  name: hackathon-app

variables:
  sql_warehouse_id:
    description: SQL Warehouse ID
  postgres_branch:
    description: Full Lakebase Postgres branch resource name. Obtain by running `databricks postgres list-branches {project-name}`, select the desired item from the output array and use its .name value. Requires the project resource name.
  postgres_database:
    description: Full Lakebase Postgres database resource name. Obtain by running `databricks postgres list-databases {branch-name}`, select the desired item from the output array and use its .name value. Requires the branch resource name.

resources:
  apps:
    app:
      name: "hackathon-app"
      description: "DAIS 2026 hackathon app with analytics and Lakebase scenarios"
      source_code_path: ./
      user_api_scopes:
        - postgres

      resources:
        - name: sql-warehouse
          sql_warehouse:
            id: ${var.sql_warehouse_id}
            permission: CAN_USE
        - name: postgres
          postgres:
            branch: ${var.postgres_branch}
            database: ${var.postgres_database}
            permission: CAN_CONNECT_AND_CREATE

targets:
  default:
    default: true
    workspace:
      host: https://dbc-13a5ebc6-7820.cloud.databricks.com

    variables:
      sql_warehouse_id: d8c2d376825167df
      postgres_branch: projects/hackathon-app/branches/production
      postgres_database: projects/hackathon-app/branches/production/databases/databricks-postgres
